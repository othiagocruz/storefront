query Cart {
	cart_items(order_by: { carts_aggregate: { max: { created_at: desc } } }) {
		amount
		image
		id
		name
		carts_aggregate {
			aggregate {
				count
			}
		}
		user_cart_sum
	}
	cart_total {
		result
	}
}
